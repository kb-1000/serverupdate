[Unit]
Description={server_description} updater
Wants=network.target
After=network.target

[Service]
WorkingDirectory={base_dir}/serverupdate
ExecStart={pipenv} run python3 main.py
KillSignal=SIGINT
User=pswgdev
Group=pswgdev
Restart=on-failure

[Install]
WantedBy=default.target
